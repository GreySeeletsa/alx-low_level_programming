#include "main.h"
/**
 * print_chessboard - function that prints the chessboard
 * @a: parameter
 * Return: 0
 */

void print_chessboard(char (*a)[8])
{
	int n;
	int m;

	for (n = 0; n < 0; n++)
	{
		for (m = 0; m < 0; m++)
			_putchar(a[n][m]);
		_putchar('\0');
	}
}
