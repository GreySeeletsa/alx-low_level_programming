#include "main.h"
#include <stdio.h>
/**
 * add -  function that adds two integers and returns the result
 * @n: first parameter
 * @m: second parameter
 * Return: value
 */

int add(int n, int m)
{
	return (n + m);
}
